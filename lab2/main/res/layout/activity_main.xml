<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

    <Button
            android:id="@+id/loadButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Загрузить данные"
            android:padding="16dp"
            android:layout_marginLeft="2dp"
            android:layout_centerHorizontal="@id/titleTextView"
            android:layout_marginTop="16dp" />

    <EditText
            android:id="@+id/jsonUrlEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Введите URL JSON-файла"
            android:layout_centerHorizontal="true"
            android:layout_below="@id/loadButton"
            android:layout_marginTop="16dp" />

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/jsonUrlEditText"
            android:layout_marginTop="8dp" />

    <TextView
            android:id="@+id/titleTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/loadButton"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginLeft="12dp"
            android:text="Разарботал\n Марцинкевич Илья" />

</RelativeLayout>
