<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:icon="@drawable/ic_launcher_background"
        android:label="YljanaVasilueva" >
        <activity
            android:name=".Start"
            android:label="Memoria"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name=".MainActivity" android:configChanges="orientation"> </activity>
        <activity android:label="@string/app_name" android:name=".Settings"> </activity>
        <activity android:label="@string/app_name" android:name=".Records"> </activity>
        <activity android:label="@string/app_name" android:name=".RecordPoint"> </activity>
        <activity android:label="@string/app_name" android:name=".RecordTime"> </activity>

    </application>
</manifest>

